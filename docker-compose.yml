services:
  astro:
    build:
      context: .
    ports:
      - "4173:4173"
    volumes:
      - ./:/app:cached
      - node_modules:/app/node_modules
    command: npm run dev -- --host 0.0.0.0 --port 4173
    environment:
      NODE_ENV: development

volumes:
  node_modules:
